info:
  name: libros filter
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5001/api/libros?categoria=fic
  params:
    - name: categoria
      value: fic
      type: query
    - name: search
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
