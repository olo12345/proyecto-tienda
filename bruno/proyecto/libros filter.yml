info:
  name: libros filter
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5001/api/libros/filter?search=harr
  params:
    - name: search
      value: harr
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
